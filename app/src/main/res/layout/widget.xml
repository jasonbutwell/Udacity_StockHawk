<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:orientation="vertical">
    <TextView
        android:id="@+id/StockHawkTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="StockHawk"
        android:text="@string/widget_title"
        style="@style/StockHawkWidgetHeaderStyle"
        />
    <ListView
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/widget_margin"
        android:layout_marginStart="@dimen/widget_margin"
        android:layout_marginLeft="@dimen/widget_margin"
        android:layout_marginEnd="@dimen/widget_margin"
        android:layout_marginRight="@dimen/widget_margin"
        android:layout_below="@+id/StockHawkTitle"/>
    <TextView
        android:id="@+id/listViewEmpty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_gravity="center"
        android:text="@string/label_widget_nostocks"
        android:visibility="gone"
        tools:text="Empty list" />
</RelativeLayout>
